/**
 * Admin panel i18n helper.
 *
 * The admin panel uses English as its sole UI language.
 * All user-facing strings are written directly in English throughout the
 * component and page files — no runtime translation layer is needed here.
 *
 * This module is kept as a placeholder to align with the monorepo structure
 * and to make it easy to introduce a full i18n solution in the future.
 */

export const ADMIN_LOCALE = "en" as const;
